bot.phosphorcode.com.br {
	# Expõe publicamente apenas o formulário e o health check.
	@pub path /webhook/form /health
	handle @pub {
		reverse_proxy antonela:3000
	}
	# Qualquer outra rota (webhook da Evolution, admin) fica bloqueada externamente.
	handle {
		respond "Not found" 404
	}
}
